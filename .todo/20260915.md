# Hermes UI TODO — 完成归档 2026-09-15

> 规则：条目收口时从 `.todo/active.md` 誊写完整条目到本文件，标题标 `[已收口]`。

---

## [已收口] #115 新建会话自动打开上下文弹窗：抢在页面入场滑动（300ms）中途弹出 → 弹窗冻结在半程锚点坐标（主人 2026-09-15 报告）

- 位置（修复点）：
  - `lib/features/chat/widgets/chat_input_bar.dart`：新增 `_entranceTransitionTimeout`(600ms) / `_awaitEntranceTransition()` / `_settleAfterTransition()` / `_autoOpenContextPopover()`；`_tryAutoOpenContextPopover()`（initState 首个 postFrame 路径）与 `_bindAutoOpenListener()`（首个 snapshot 到达路径）均改为 `unawaited(_autoOpenContextPopover())`。手动点按指示器（`onTap: _showContextPopover`）路径**零改动**。
  - 未改：`adaptive_popover.dart`（仍为一次性锚点快照，无 LayerLink 跟随——方案 B 已评估为非本次必需）、路由转场 300ms、`markCreated` 标记/清除时序、`session_list_page.dart` 业务流程。
- 根因链（源码级 + widget 探针取证）：
  1. 弹层定位在 `showAdaptivePopover`（`lib/app/widgets/adaptive_popover.dart:88-115`）调用瞬间用 `anchorBox.localToGlobal(offset, ancestor: overlayBox)` **快照**锚点矩形，`_AdaptivePopoverHost` 用静态 `Positioned` 布局、无跟随重建 → 坐标一旦算出即冻结。
  2. 转场为 `HermesPageRoute` 300ms `Curves.easeOut` 纯水平滑入（`hermes_page_route.dart:22-88`），`/chat/:sessionId` 走 `HermesPage`（`router.dart:106-114`），宽屏同一转场（`adaptive_shell.dart:280-295`）。
  3. 自动打开抢在转场中途触发 → 弹窗定格在「半程」指示器坐标。
- 关键实测（widget 探针逐帧，1200×900、页面左留 400 模拟宽屏双栏）：指示器静止态 `left=496`；150ms 帧 `left=875.7`；动画 `value` 到 1.0 的那一帧 status 仍为 `forward`，`completed` 在**下一帧 tick 阶段**置位，而 **status 置位瞬间渲染树几何仍是两帧前的**（快照读到的坐标 = `left 549.11`，即残余位移 53.11px）。故「只等 status==completed」不够——多补 `endOfFrame` × 2 后，自动打开坐标 (281, 539) 与静止后手动打开坐标**完全一致**（修复前为 334.1/592.1，偏 53.11px）。
- 更正前次记录：本轮早期 TODO 曾写「宽屏被 clamp 到屏幕右缘」——**不成立**。指示器位于输入栏**左簇**（附件/收藏/上下文指示器），`PopoverAlign.end` 下 `left = anchor.right - 260`，半程位移不会被右缘 clamp 吸收；实际表现是**整段右移**（桌面 1200 宽详情区半程 ≈ 300-380px）。窄屏同样错位（并非「仅几像素」）。
- 测试（红→绿取证）：
  - 新增 `test/features/chat/chat_auto_open_context_transition_test.dart`：① `HermesPageRoute` push 入聊天页，`pump(150ms)` 断言 `AdaptivePopover.activeOverlayCount == 0` 且无弹层 → `pumpAndSettle` 后断言弹出；再以「自动打开 rect == 关闭后手动点击打开 rect」做位置等价断言。② `home:` 首屏（`didAdd` 已 completed）60ms 内即弹出，守住「不等」分支不被兜底超时拖延。
  - 修复前跑该用例：`Expected: <0> Actual: <1>`（150ms 处弹层已出现）→ 修复后两条全绿。
  - 全量：`C:/tmp/f.bat analyze` → `No issues found!`（exit 0）；`C:/tmp/f.bat test` → `All tests passed!`（+2925 ~8，exit 0）。
- 验收（**待主人实机复验**）：桌面宽窗开「新建会话自动打开上下文」→ 新建会话，弹窗在页面滑入**结束之后**出现、与指示器静止态对齐（不再停在中途）；窄屏同流程无错位；手动点指示器立即弹出（无新增延迟）。
- 状态：已修复（本批 commit），待主人实机复验。

---
